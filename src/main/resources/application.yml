spring:
  application:
    name: movieflix

  datasource:
    url: jdbc:postgresql://localhost:5432/mvxdb
    username: admin
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

#  hibernate:
#    ddl-auto: create

  flyway:
    enabled: true

movieflix:
  security:
    secret: "oRhaC9PtDFVTC7qpqJKTv//j6TmqAFAdQQqshE0P6I="
    #temporary disposable secret key for testing purposes only
    #generated with ~ openssl rand -base64 32